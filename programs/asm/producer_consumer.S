# Producer writes a sequence of data words followed by a publication flag.
# Consumer polls the flag, reads the published data, and accumulates a result.
# See tb/soc/producer_consumer_tb.sv for the executable encoded program.
